name: nc-differentiation

services:
  web:
    build: .
    container_name: nc-differentiation-web
    restart: unless-stopped
    networks:
      - proxy
    expose:
      - "80"

networks:
  proxy:
    external: true
